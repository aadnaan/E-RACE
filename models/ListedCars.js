class Product {
    constructor(id, ownerId, title, imageUrl, year, price) {
      this.id = id;
      this.ownerId = ownerId;
      this.imageUrl = imageUrl;
      this.title = title;
      this.year = year;
      this.price = price;
    }
  }
  
  export default Product;
  